<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hemanth.kotlinCoroutines">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KotlinCoroutines">
        <activity android:name=".eachCoroutineExample.retrofit.single.SingleNetworkCallActivity"/>

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".eachCoroutineExample.retrofit.series.SeriesNetworkCallsActivity" />
        <activity android:name=".eachCoroutineExample.retrofit.parallel.ParallelNetworkCallsActivity" />
        <activity android:name=".eachCoroutineExample.room.RoomDBActivity" />

    </application>

</manifest>